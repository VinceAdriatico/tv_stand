# General Three.js Boilerplate

## How to install
`npm install`

## Runtime
### Run dev server
`npm run dev`

### Publish on Pages
`npm run deploy`

## Production Steps
Vite is configured via `vite.config.js` with `base: './'` for relative pathing. No manual path changes needed for GH-Pages.